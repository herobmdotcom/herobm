{
  "name": "modbm-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1"
  },
  "scripts": {
    "seed": "npm run seed -w apps/api",
    "setup": "npm run setup -w api"
  }
}
